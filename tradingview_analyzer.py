import requests
from datetime import datetime, timedelta
from config import OANDA_API_KEY
import json
import pandas as pd
import ta

class TradingViewAnalyzer:
    """Analyzes TradingView data and charts"""
    
    def __init__(self):
        self.tradingview_api = "https://www.tradingview.com/api/v1"
        self.chart_data_cache = {}
    
    def get_tradingview_chart(self, symbol, interval="1h", count=100):
        """
        Fetch chart data compatible with TradingView
        
        Args:
            symbol: Forex pair (e.g., "EUR_USD", "EURUSD")
            interval: Timeframe (1m, 5m, 15m, 1h, 4h, 1d, 1w, 1M)
            count: Number of candles to fetch
        
        Returns:
            DataFrame with OHLCV data
        \"\"\"\n        try:\n            # Normalize symbol format\n            symbol = symbol.replace(\"_\", \"\").upper()\n            \n            # Map timeframes\n            timeframe_map = {\n                \"1m\": \"M1\",\n                \"5m\": \"M5\",\n                \"15m\": \"M15\",\n                \"1h\": \"H1\",\n                \"4h\": \"H4\",\n                \"1d\": \"D1\",\n                \"1w\": \"W1\",\n                \"1M\": \"MN1\"\n            }\n            \n            tv_interval = timeframe_map.get(interval, \"H1\")\n            \n            # Format as OANDA pair\n            oanda_pair = self._convert_to_oanda_format(symbol)\n            \n            # Fetch data\n            df = self._fetch_oanda_data(oanda_pair, tv_interval, count)\n            \n            if df is not None:\n                self.chart_data_cache[f\"{symbol}_{interval}\"] = df\n            \n            return df\n        \n        except Exception as e:\n            print(f\"Error fetching TradingView chart: {e}\")\n            return None\n    \n    def _convert_to_oanda_format(self, symbol):\n        \"\"\"Convert symbol to OANDA format (e.g., EURUSD -> EUR_USD)\"\"\"\n        if len(symbol) == 6:\n            return f\"{symbol[:3]}_{symbol[3:]}\"\n        return symbol\n    \n    def _fetch_oanda_data(self, pair, interval, count):\n        \"\"\"Fetch data from OANDA (TradingView compatible)\"\"\"\n        try:\n            base_url = \"https://api-fxpractice.oanda.com\"\n            headers = {\n                \"Authorization\": f\"Bearer {OANDA_API_KEY}\",\n                \"Content-Type\": \"application/json\"\n            }\n            \n            url = f\"{base_url}/v3/instruments/{pair}/candles\"\n            params = {\n                \"count\": count,\n                \"granularity\": interval,\n                \"price\": \"MBA\"\n            }\n            \n            response = requests.get(url, headers=headers, params=params)\n            \n            if response.status_code == 200:\n                data = response.json()\n                return self._parse_candles(data)\n            else:\n                return None\n        \n        except Exception as e:\n            print(f\"Error fetching OANDA data: {e}\")\n            return None\n    \n    def _parse_candles(self, data):\n        \"\"\"Parse candle data into DataFrame\"\"\"\n        candles = data.get(\"candles\", [])\n        df_data = []\n        \n        for candle in candles:\n            df_data.append({\n                \"time\": candle[\"time\"],\n                \"open\": float(candle[\"mid\"][\"o\"]),\n                \"high\": float(candle[\"mid\"][\"h\"]),\n                \"low\": float(candle[\"mid\"][\"l\"]),\n                \"close\": float(candle[\"mid\"][\"c\"]),\n                \"volume\": candle[\"volume\"]\n            })\n        \n        df = pd.DataFrame(df_data)\n        df[\"time\"] = pd.to_datetime(df[\"time\"])\n        return df\n    \n    def analyze_tradingview_chart(self, symbol, interval=\"1h\"):\n        \"\"\"Complete TradingView chart analysis\"\"\"\n        try:\n            # Fetch chart data\n            df = self.get_tradingview_chart(symbol, interval)\n            \n            if df is None or len(df) < 30:\n                return {\"error\": f\"Insufficient data for {symbol}\"}\n            \n            # Calculate indicators\n            indicators = self._calculate_tv_indicators(df)\n            \n            # Identify patterns\n            patterns = self._identify_chart_patterns(df)\n            \n            # Generate analysis\n            analysis = {\n                \"symbol\": symbol,\n                \"timeframe\": interval,\n                \"current_price\": df[\"close\"].iloc[-1],\n                \"indicators\": indicators,\n                \"patterns\": patterns,\n                \"support_resistance\": self._calculate_support_resistance(df),\n                \"trend\": self._identify_trend(df),\n                \"momentum\": self._calculate_momentum(df)\n            }\n            \n            return analysis\n        \n        except Exception as e:\n            return {\"error\": str(e)}\n    \n    def _calculate_tv_indicators(self, df):\n        \"\"\"Calculate TradingView popular indicators\"\"\"\n        indicators = {}\n        \n        try:\n            # RSI\n            indicators[\"rsi\"] = ta.momentum.rsi(df[\"close\"], window=14).iloc[-1]\n            \n            # MACD\n            macd = ta.trend.macd(df[\"close\"])\n            indicators[\"macd\"] = macd.iloc[-1]\n            indicators[\"macd_signal\"] = ta.trend.macd_signal(df[\"close\"]).iloc[-1]\n            indicators[\"macd_histogram\"] = (macd - ta.trend.macd_signal(df[\"close\"])).iloc[-1]\n            \n            # Bollinger Bands\n            bb = ta.volatility.bollinger_bands(df[\"close\"], window=20)\n            indicators[\"bb_upper\"] = bb.iloc[-1, 0]\n            indicators[\"bb_middle\"] = bb.iloc[-1, 1]\n            indicators[\"bb_lower\"] = bb.iloc[-1, 2]\n            \n            # Moving Averages\n            indicators[\"sma_20\"] = ta.trend.sma(df[\"close\"], window=20).iloc[-1]\n            indicators[\"sma_50\"] = ta.trend.sma(df[\"close\"], window=50).iloc[-1]\n            indicators[\"sma_200\"] = ta.trend.sma(df[\"close\"], window=200).iloc[-1]\n            indicators[\"ema_12\"] = ta.trend.ema(df[\"close\"], window=12).iloc[-1]\n            indicators[\"ema_26\"] = ta.trend.ema(df[\"close\"], window=26).iloc[-1]\n            \n            # Stochastic\n            stoch = ta.momentum.stoch(df[\"high\"], df[\"low\"], df[\"close\"], window=14)\n            indicators[\"stoch_k\"] = stoch.iloc[-1, 0]\n            indicators[\"stoch_d\"] = stoch.iloc[-1, 1]\n            \n            # ATR (Volatility)\n            indicators[\"atr\"] = ta.volatility.average_true_range(\n                df[\"high\"], df[\"low\"], df[\"close\"], window=14\n            ).iloc[-1]\n            \n            # Volume\n            indicators[\"avg_volume\"] = df[\"volume\"].tail(20).mean()\n            indicators[\"current_volume\"] = df[\"volume\"].iloc[-1]\n            \n        except Exception as e:\n            print(f\"Error calculating indicators: {e}\")\n        \n        return indicators\n    \n    def _identify_chart_patterns(self, df):\n        \"\"\"Identify candlestick and chart patterns\"\"\"\n        patterns = []\n        \n        try:\n            last_close = df[\"close\"].iloc[-1]\n            last_open = df[\"open\"].iloc[-1]\n            last_high = df[\"high\"].iloc[-1]\n            last_low = df[\"low\"].iloc[-1]\n            prev_close = df[\"close\"].iloc[-2]\n            \n            # Doji (equal open and close)\n            if abs(last_close - last_open) < (last_high - last_low) * 0.1:\n                patterns.append({\"name\": \"Doji\", \"signal\": \"Indecision\", \"strength\": 0.6})\n            \n            # Hammer (small body, long lower wick)\n            body = abs(last_close - last_open)\n            lower_wick = last_open - last_low if last_open < last_close else last_close - last_low\n            if lower_wick > body * 2:\n                patterns.append({\"name\": \"Hammer\", \"signal\": \"Bullish Reversal\", \"strength\": 0.7})\n            \n            # Shooting Star (small body, long upper wick)\n            upper_wick = last_high - last_close if last_open < last_close else last_high - last_open\n            if upper_wick > body * 2:\n                patterns.append({\"name\": \"Shooting Star\", \"signal\": \"Bearish Reversal\", \"strength\": 0.7})\n            \n            # Engulfing patterns\n            if last_close > df[\"open\"].iloc[-2] and last_open < df[\"close\"].iloc[-2]:\n                patterns.append({\"name\": \"Bullish Engulfing\", \"signal\": \"Strong Bullish\", \"strength\": 0.8})\n            elif last_close < df[\"open\"].iloc[-2] and last_open > df[\"close\"].iloc[-2]:\n                patterns.append({\"name\": \"Bearish Engulfing\", \"signal\": \"Strong Bearish\", \"strength\": 0.8})\n        \n        except Exception as e:\n            print(f\"Error identifying patterns: {e}\")\n        \n        return patterns\n    \n    def _calculate_support_resistance(self, df, lookback=50):\n        \"\"\"Calculate support and resistance levels\"\"\"\n        try:\n            recent_data = df.tail(lookback)\n            \n            resistance = recent_data[\"high\"].max()\n            support = recent_data[\"low\"].min()\n            \n            current_price = df[\"close\"].iloc[-1]\n            \n            # Mid-level\n            mid_level = (resistance + support) / 2\n            \n            return {\n                \"resistance\": resistance,\n                \"support\": support,\n                \"middle\": mid_level,\n                \"distance_to_resistance\": resistance - current_price,\n                \"distance_to_support\": current_price - support\n            }\n        except Exception as e:\n            print(f\"Error calculating S/R: {e}\")\n            return {}\n    \n    def _identify_trend(self, df):\n        \"\"\"Identify current trend\"\"\"\n        try:\n            sma_20 = ta.trend.sma(df[\"close\"], window=20).iloc[-1]\n            sma_50 = ta.trend.sma(df[\"close\"], window=50).iloc[-1]\n            sma_200 = ta.trend.sma(df[\"close\"], window=200).iloc[-1]\n            current_price = df[\"close\"].iloc[-1]\n            \n            if current_price > sma_20 > sma_50 > sma_200:\n                return {\"trend\": \"Strong Uptrend\", \"strength\": 0.9}\n            elif current_price > sma_20 > sma_50:\n                return {\"trend\": \"Uptrend\", \"strength\": 0.7}\n            elif current_price < sma_20 < sma_50 < sma_200:\n                return {\"trend\": \"Strong Downtrend\", \"strength\": 0.9}\n            elif current_price < sma_20 < sma_50:\n                return {\"trend\": \"Downtrend\", \"strength\": 0.7}\n            else:\n                return {\"trend\": \"Sideways/Consolidation\", \"strength\": 0.5}\n        \n        except Exception as e:\n            print(f\"Error identifying trend: {e}\")\n            return {\"trend\": \"Unknown\", \"strength\": 0}\n    \n    def _calculate_momentum(self, df):\n        \"\"\"Calculate momentum indicators\"\"\"\n        try:\n            rsi = ta.momentum.rsi(df[\"close\"], window=14).iloc[-1]\n            macd = ta.trend.macd(df[\"close\"]).iloc[-1]\n            macd_signal = ta.trend.macd_signal(df[\"close\"]).iloc[-1]\n            \n            momentum = {\n                \"rsi_value\": rsi,\n                \"rsi_status\": \"Overbought\" if rsi > 70 else (\"Oversold\" if rsi < 30 else \"Neutral\"),\n                \"macd_status\": \"Bullish\" if macd > macd_signal else \"Bearish\",\n                \"price_momentum\": (df[\"close\"].iloc[-1] - df[\"close\"].iloc[-5]) / df[\"close\"].iloc[-5] * 100\n            }\n            \n            return momentum\n        except Exception as e:\n            print(f\"Error calculating momentum: {e}\")\n            return {}\n    \n    def multi_timeframe_analysis(self, symbol):\n        \"\"\"Analyze chart on multiple timeframes (like TradingView)\"\"\"\n        timeframes = [\"1h\", \"4h\", \"1d\", \"1w\"]\n        analysis = {}\n        \n        for tf in timeframes:\n            analysis[tf] = self.analyze_tradingview_chart(symbol, tf)\n        \n        return analysis
